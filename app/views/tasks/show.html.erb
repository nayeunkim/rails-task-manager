<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to "Update task", edit_task_path %>
<%= link_to "Back to list", tasks_path %>
<%= link_to "Delete",
            task_path(@task.id),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
